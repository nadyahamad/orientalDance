<%- include('../partials/head.ejs') %>
    <body>
        <%- include('../partials/header.ejs') %>
        <!-- main content -->
        <main class="content">
            <!-- tabs -->
            <section class="container mt-2 mb-1">
                    <!-- form card-->
                    <div class="col-7 col-s-10 signin-form" novalidate>
                        <h1 class="tex-center"> <%= title %> </h1>
                        <p class="text-center ml-2"> Please enter your details </p>
                        <!-- form Login -->
                        <% if (errorMessage) { %>
                            <div class="user-message user-message--error" ><%= errorMessage %></div>
                        <% } %>
                        <form method="POST" action='/reset' novalidate>
                            <div class="padding-1">
                                <label for="email">Email</label>
                                <input type="email" name="email"  id="email" placeholder="email" class="form-inputs" />
                            </div>
                            <div class="padding-1 container">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <button class="large-btn" type="submit">change Password</button>
                            </div>
                        </form>
                    </div>
                    <!-- form card -->
                </div>
            </section>
            <!-- tabs -->
        </main>
        <!-- main content -->
        <%- include('../partials/footer.ejs') %>
        <%- include('../partials/end.ejs') %>
    </body>
</html>